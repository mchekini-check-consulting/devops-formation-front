trigger: none

pr:
  branches:
    include:
    - main

pool:
  vmImage: ubuntu-latest

variables:
- group: variable

stages:

# ─────────────────────────────────────────────
# STAGE 0 : Setup – Compute Build Number
# ─────────────────────────────────────────────
- stage: SetupTag
  displayName: 'Setup – Compute Build Number'
  jobs:
  - job: ComputeTag
    steps:
    - checkout: self
      fetchDepth: 0

    - script: |
        set -e
        SHORT_SHA=$(echo "$(Build.SourceVersion)" | cut -c1-7)

        RAW_BRANCH="${SYSTEM_PULLREQUEST_TARGETBRANCH:-$(Build.SourceBranchName)}"
        BRANCH=$(echo "$RAW_BRANCH" | sed 's|refs/heads/||')

        BRANCH_SLUG=$(echo "$BRANCH" \
          | tr '[:upper:]' '[:lower:]' \
          | sed 's|[^a-z0-9]|-|g; s|-\+|-|g; s|^-||; s|-$||' \
          | cut -c1-20)

        TIMESTAMP=$(date -u +%Y%m%d%H%M)
        BUILD_NUMBER="${SHORT_SHA}-${BRANCH_SLUG}-${TIMESTAMP}"

        echo "##vso[build.updatebuildnumber]$BUILD_NUMBER"
      displayName: 'Compute build number (sha-branchslug-ts)'
      env:
        SYSTEM_PULLREQUEST_TARGETBRANCH: $(System.PullRequest.TargetBranch)

# ─────────────────────────────────────────────
# STAGE 1 : Lint
# ─────────────────────────────────────────────
- stage: Lint
  displayName: 'Lint'
  dependsOn: SetupTag
  jobs:
  - job: LintJob
    steps:
    - task: NodeTool@0
      inputs:
        versionSpec: '20.x'

    - task: Npm@1
      inputs:
        command: install

    - task: Npm@1
      inputs:
        command: custom
        customCommand: 'run lint'

# ─────────────────────────────────────────────
# STAGE 2 : Gitleaks – Secret Scanning
# ─────────────────────────────────────────────
- stage: Gitleaks
  displayName: 'Gitleaks – Secret Scanning'
  dependsOn: Lint
  jobs:
  - job: GitleaksJob
    steps:
    - checkout: self
      fetchDepth: 0

    - script: |
        set -e
        GITLEAKS_VERSION="8.18.0"
        wget -q https://github.com/gitleaks/gitleaks/releases/download/v${GITLEAKS_VERSION}/gitleaks_${GITLEAKS_VERSION}_linux_x64.tar.gz
        tar -xzf gitleaks_${GITLEAKS_VERSION}_linux_x64.tar.gz
        chmod +x gitleaks
        ./gitleaks detect --source . --verbose --exit-code 1
      displayName: 'Run Gitleaks'

# ─────────────────────────────────────────────
# STAGE 3 : Unit Tests
# ─────────────────────────────────────────────
- stage: UnitTests
  displayName: 'Unit Tests'
  dependsOn: Gitleaks
  jobs:
  - job: UnitTestsJob
    steps:
    - task: NodeTool@0
      inputs:
        versionSpec: '20.x'

    - task: Npm@1
      inputs:
        command: install

    - task: Npm@1
      inputs:
        command: custom
        customCommand: 'test -- --ci --reporters=default --reporters=jest-junit'
      env:
        CI: 'true'
        JEST_JUNIT_OUTPUT_DIR: '$(System.DefaultWorkingDirectory)/test-results'
        JEST_JUNIT_OUTPUT_NAME: 'junit.xml'

    - task: PublishTestResults@2
      condition: always()
      inputs:
        testResultsFormat: JUnit
        testResultsFiles: '**/test-results/junit.xml'

    - task: PublishBuildArtifacts@1
      displayName: 'Artifact – coverage-report'
      condition: always()
      inputs:
        PathtoPublish: '$(System.DefaultWorkingDirectory)/coverage'
        ArtifactName: 'coverage-report'

    - task: PublishCodeCoverageResults@2
      displayName: 'Publish Code Coverage'
      condition: always()
      inputs:
        summaryFileLocation: '$(System.DefaultWorkingDirectory)/coverage/cobertura-coverage.xml'

# ─────────────────────────────────────────────
# STAGE 4 : SonarQube Analysis
# ─────────────────────────────────────────────
- stage: Sonar
  displayName: 'SonarQube Analysis'
  dependsOn: UnitTests
  jobs:
  - job: SonarJob
    steps:
    - checkout: self
      fetchDepth: 0

    - task: DownloadBuildArtifacts@1
      displayName: 'Download coverage report'
      inputs:
        artifactName: 'coverage-report'
        downloadPath: '$(System.DefaultWorkingDirectory)'

    - task: NodeTool@0
      inputs:
        versionSpec: '20.x'

    - task: Npm@1
      displayName: 'Install sonar-scanner'
      inputs:
        command: custom
        customCommand: 'install -g sonarqube-scanner'

    - task: Npm@1
      displayName: 'npm install'
      inputs:
        command: install

    - script: |
        ls -la $(System.DefaultWorkingDirectory)/coverage-report/
        sonar-scanner \
          -Dsonar.host.url=$SONAR_HOST_URL \
          -Dsonar.token=$SONAR_TOKEN \
          -Dsonar.javascript.lcov.reportPaths=$(System.DefaultWorkingDirectory)/coverage-report/lcov.info \
          -Dsonar.qualitygate.wait=true
      displayName: 'SonarQube Scan'
      env:
        SONAR_HOST_URL: $(SONAR_HOST_URL)
        SONAR_TOKEN: $(SONAR_TOKEN)

# ─────────────────────────────────────────────
# STAGE 5 : Mutation Tests (Stryker)
# ─────────────────────────────────────────────
- stage: MutationTests
  displayName: 'Mutation Tests'
  dependsOn: Sonar
  jobs:
  - job: MutationTestsJob
    steps:
    - task: NodeTool@0
      inputs:
        versionSpec: '20.x'

    - task: Npm@1
      inputs:
        command: install

    - task: Npm@1
      displayName: 'Stryker – Mutation Testing'
      inputs:
        command: custom
        customCommand: 'run mutation-test'

    - task: PublishBuildArtifacts@1
      displayName: 'Artifact – stryker-report'
      condition: always()
      inputs:
        PathtoPublish: '$(System.DefaultWorkingDirectory)/reports/mutation/mutation.html'
        ArtifactName: 'mutation-test-report'
